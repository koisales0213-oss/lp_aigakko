# 作業ログ / 進捗メモ

このファイルは、作業を中断・再開するための記録です。
作業を進めたら、その都度このファイルを更新してコミットしてください。

---

## プロジェクト概要

- **目的**: AIの学校の演習として LP（ランディングページ）を制作する
- **リポジトリ**: https://github.com/koisales0213-oss/lp_aigakko （Private）
- **ローカル作業ディレクトリ**: `~/Desktop/LP制作_AIの学校演習`

---

## 現在のステータス

**LP を GitHub Pages で公開中。** 事例セクション（実物の手紙・返信、匿名化済み）を掲載済み。
フォーム送信先・法人情報が未対応。

🔗 公開URL： https://koisales0213-oss.github.io/lp_aigakko/

---

## 作業履歴

### 2026-07-28 — LP初版を制作（大手飲食チェーン開拓用）

- 対象：**大手飲食チェーン本部を開拓したい事業者**向けの、かざあな隊 業界特化LP
- Google Drive / Notion の既存資料からサービス内容・料金・トークを収集し、コピーの根拠とした
- 素の HTML / CSS / JavaScript で1ページ完結のLPを実装（外部CDN・外部フォントなし）
- 全13セクション：ヒーロー／課題／手法3ステップ／強み2点／数字・反応／相性／料金／オプション／流れ／FAQ／CTAフォーム
- レスポンシブ確認済み（320px・390px・430px・1280px で表示検証、横スクロールなし）
- 設計意図とコピーの出典は `docs/LP設計メモ.md` に記録

### 2026-07-28 — GitHub Pages で公開

- 無料プラン＋Privateリポジトリでは Pages が使えないため、リポジトリを **Public** に変更
- 公開前に以下を実施
  - `docs/LP設計メモ.md` から、掲載許諾が未確認のクライアント実名を削除
  - コミット履歴を作り直し（旧履歴には実名と個人メールアドレスが含まれていたため）
  - コミットの作者アドレスを GitHub の noreply アドレスに変更
- `main` ブランチのルート（`/`）を公開ソースに設定し、ビルド完了を確認
- 公開URL： https://koisales0213-oss.github.io/lp_aigakko/

> ⚠️ リポジトリは Public です。**社外に出せない情報をコミットしないでください。**
> 内部向けの詳細（クライアント実名・受注金額など）は Notion 側で管理します。

### 2026-07-28 — 映像生成プロンプトを追加

- ファーストビュー背景／SNS広告用の動画生成プロンプトを `docs/映像生成プロンプト.md` に保存
- コンセプト：ペイン可視化（届かない営業）→ 一通の手紙が壁を越える → 決裁者から返信が届く
- 動画そのものは未生成。生成前に要検討の論点を同ファイル「実行メモ」に記載

### 2026-07-28 — 【事故】素材原本を公開リポジトリに誤コミット

- `git add -A` により、マスキング前の素材原本2点を公開リポジトリにコミット・push してしまった
  - 返信メールのスクリーンショットに、先方担当者とクライアント代表者のメールアドレスが写っていた
- 公開されていた時間は約9分。コミットSHAを知らないと到達できない状態だった
- 対応
  - 履歴を作り直して該当ファイルを除去
  - 原本を `_private/` に隔離、`.gitignore` に `/_private/` とルート直下の画像除外を追加
  - リポジトリを Private 化し、Pages を停止
  - force push ではダングリングオブジェクトが残るため、**リポジトリを削除して同名で作り直した**
  - 3経路（Pages / raw の main / raw の旧コミットSHA）すべてで 404 になることを確認済み
- 再発防止ルールは `docs/LP設計メモ.md` の「4-2. 素材の取り扱いルール」に記載

### 2026-07-28 — 事例セクションを追加（匿名化）

- 実物の封筒写真と返信メールを使った事例セクション（`#case`）を実装
- 掲載許諾が未取得のため、社名・宛名・所在地・担当者名・特定可能な施策名をすべて**画素ごと塗りつぶし**
- 役職「代表取締役会長 兼 社長CEO」は残した（社長宛に送った事実が訴求の核のため）
- 許諾が取れたら `_private/` の原本から実名版を作り直して差し替える

### 2026-07-28 — リポジトリを作り直して再公開

- リポジトリを削除し、同名で再作成してクリーンな履歴を push
- GitHub Pages を再有効化し、ビルド完了・全ファイル配信を確認
- 旧原本が 3経路すべてで 404 になることを確認
- 公開URL は元のまま： https://koisales0213-oss.github.io/lp_aigakko/


### 2026-07-28 — 環境セットアップ

- `git init`（デフォルトブランチ `main`）でローカルリポジトリを初期化
- Git ユーザー情報を **このリポジトリのみ** に設定（グローバル設定は未変更）
  - `user.name` = `koisales0213-oss`
  - `user.email` = GitHub の noreply アドレス（公開リポジトリのため個人アドレスは使用しない）
- リモート `origin` に GitHub リポジトリを登録
- 認証は GitHub CLI（`gh`）の credential helper 経由。push 時のパスワード入力は不要
- 以下のファイルを作成し、初回コミットを push
  - `.gitignore` — macOS の `.DS_Store`、エディタ設定、`node_modules/`、`dist/`、`.env` などを除外
  - `README.md` — リポジトリの説明
- `main` が `origin/main` を追跡する状態に設定済み

---

## 次にやること（TODO）

- [x] LP の要件を決める（誰に向けた、何の LP か／掲載する情報／ゴールとなる行動）
- [x] 構成（セクション構成）を決める
- [x] 技術スタックを決める（素の HTML/CSS/JS）
- [x] LP の実装（初版）
- [ ] **フォームの送信先を設定**（`assets/js/main.js` の `FORM_ENDPOINT` が空のため現状送信不可）
- [ ] 会社情報（社名・所在地・連絡先）とプライバシーポリシーのリンクを入れる
- [ ] 実績セクションを実名に差し替え（各社の掲載許諾が取れたら）
- [ ] OGP画像・ファビコンの用意
- [x] 公開方法を決める（GitHub Pages で公開済み）
- [ ] ヒーロー背景の動画を生成する（プロンプトは `docs/映像生成プロンプト.md`）
- [ ] 動画を採用する場合、`.hero__bg` を `<video>` に差し替える改修

詳細は `docs/LP設計メモ.md` の「5. 未対応・要対応」を参照。

---

## 再開時の手順

### 同じ Mac で続きから作業する場合

```bash
cd ~/Desktop/LP制作_AIの学校演習
git pull            # 他の環境で作業していた場合に備えて
git log --oneline   # これまでのコミットを確認
```

そのうえで、このファイルの「次にやること」から再開してください。

### 別のパソコンで作業する場合

```bash
gh auth login                                              # 未認証なら
git clone https://github.com/koisales0213-oss/lp_aigakko.git
cd lp_aigakko
```

### 作業を保存する（毎回の流れ）

```bash
git add -A
git commit -m "変更内容を書く"
git push
```

### Claude Code で再開する場合

このディレクトリで `claude` を起動し、次のように伝えてください。

> PROGRESS.md を読んで、続きから作業して
